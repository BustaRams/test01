<div class="white-bg pad-block log-wrap">
    <div class="container">
      <div class="main-title">
          <h1>Sign <span>Up</span></h1>
          <p class="tag">Easy and quick account creation fot everyone, Already have an account?<%= link_to "Log In Here" ,user_session_path , class:'link' %> </p>
      </div>
          <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <div class="frm-wrap">
              <%= f.error_notification %>
                <div class="gray-box">
                    <div class="frm-line">
                        <div class="frm-half">
                            <div class="form-group">
                              <%= f.label :First_name %>
                              <%= f.text_field :First_name, autofocus: true, class: "form-control", placeholder: "John" %>
                              <p class="error"><%= resource.errors.full_messages_for(:first_name)[0] if resource.errors[:first_name].present? %></p>
                            </div>
                        </div>
                        <div class="frm-half">
                            <div class="form-group">
                              <%= f.label :Last_name %>
                              <%= f.text_field :Last_name, autofocus: true, class: "form-control", placeholder: "Smith" %>
                              <p class="error"><%= resource.errors.full_messages_for(:Last_name)[0] if resource.errors[:Last_name].present? %></p>
                            </div>
                        </div>
                    </div>
                    <div class="frm-line">
                        <div class="frm-full">
                            <div class="form-group">
                                 <label>* Email</label>
                                  <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "user@example.com" %>
                                  <p class="error"><%= resource.errors.full_messages_for(:email)[0] if resource.errors[:email].present? %></p>

                            </div>
                        </div>
                    </div>
                    <div class="frm-line">
                        <div class="frm-half">
                            <div class="form-group">
                                <%= f.input :birthday, as: :date, start_year: Date.today.year - 90,
                                            end_year: Date.today.year - 12,
                                            order: [:day, :month, :year] %>

                            </div>
                        </div>
                        <div class="frm-half">
                            <div class="form-group">
                              <%= f.label :language %>
                              <%= f.select :language_id, Language.all.map { |r| [r.name, r.id] }, {include_blank: false}, { class: 'form-control'} %>
                              <p class="error"><%= resource.errors.full_messages_for(:language_id)[0] if resource.errors[:language_id].present? %></p>

                            </div>
                        </div>
                    </div>
                    <div class="frm-line">
                        <div class="frm-full">
                            <div class="form-group">
                              <label>* Password</label>
                              <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Minimum 6 characters" %>                            </div>
                              <p class="error"><%= resource.errors.full_messages_for(:password)[0] if resource.errors[:password].present? %></p>

                        </div>
                    </div>
                    <div class="frm-line">
                        <div class="frm-full">
                            <div class="form-group">
                                <label>* Re-enter Password</label>
                                <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "Confirm Password" %>
                                <p class="error"><%= resource.errors.full_messages_for(:password_confirmation)[0] if resource.errors[:password_confirmation].present? %></p>

                            </div>
                        </div>
                    </div>
                </div>
                <div class="checkbox text-center">
                  <%= f.label :terms_conditions do %>
                      <%= f.check_box :terms_conditions %>
                      <span></span>I agree to the <%= link_to 'Terms of Service', '/terms_and_conditions.pdf' %>.
                  <% end %>
                </div>
                <div class="btn-wrap mar two"><%= f.submit "Get Started Now" , { class: 'fill-btn' } %></div>
            </div>
            <% end %>
    </div>
</div>
